public class Main {
    public static void main(String[] args) {
        Slider slider = new Slider();
        slider.mischen(slider.sliderArray);
        slider.Spielfeld();
        slider.spielen();

    }
}
